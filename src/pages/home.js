import { SettingsContainer, HeaderContainer } from '../containers';

const Home = () => {
  return (
    <>
      <HeaderContainer />
      <SettingsContainer />
    </>
  );
};

export default Home;
